# SQL Queries

The SQL in this directory is a set of examples intended for aggregation with the DuckDB CLI, and its behavior is not guaranteed.

These queries are provided as references and may require adjustment for your local data layout or schema.
